name: Seed Blog Campaign v2

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/seed-blog-campaign-v2.yml"
      - "content/blog-campaign-2026-05-04-v2.json"
      - "content/title-candidates-2026-05-04-v2.csv"
      - "scripts/prepare-blog-campaign-v2.ts"
  workflow_dispatch:

jobs:
  seed:
    name: Seed 2 scheduled v2 blog posts
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate and insert v2 campaign
        env:
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npm run prepare:blog-campaign:v2 -- --insert
