# Google Places API 버전 비교

## 📊 두 가지 Places API

### 1. **Places API** (기존 버전)
- **장소 수**: 100 million places
- **상태**: 안정적, 널리 사용됨
- **엔드포인트**: `maps.googleapis.com/maps/api/place/`
- **문서화**: 잘 되어 있음
- **가격**: $32 (Text Search) / $17 (Details) per 1,000건

### 2. **Places API (New)** (새 버전)
- **장소 수**: 200 million places (2배)
- **상태**: Next generation, 최신 기능
- **엔드포인트**: `places.googleapis.com/places/v1/`
- **문서화**: 상대적으로 적음
- **가격**: 유사하거나 약간 다를 수 있음

## 🔍 주요 차이점

| 항목 | Places API (기존) | Places API (New) |
|------|------------------|------------------|
| 장소 데이터 | 100 million | 200 million |
| API 엔드포인트 | `/maps/api/place/` | `/places/v1/` |
| 응답 형식 | JSON (기존) | JSON (개선) |
| 필드 선택 | 제한적 | 더 유연한 필드 선택 |
| 성능 | 안정적 | 최적화됨 |
| 문서/예제 | 풍부함 | 상대적으로 적음 |

## 💡 권장 사항

### 현재 코드는 **Places API (기존)** 사용 중

**이유**:
1. ✅ 안정적이고 검증됨
2. ✅ 문서와 예제가 풍부함
3. ✅ 현재 코드와 호환됨
4. ✅ 100 million 장소로도 충분함 (한국 약국은 약 2만개)

### Places API (New)로 전환 고려 시점

다음 경우에만 고려:
- ❌ 기존 API에서 약국을 찾지 못하는 경우가 많을 때
- ❌ 더 많은 필드나 기능이 필요할 때
- ❌ Google이 기존 API 지원 중단을 발표할 때

## 🔧 현재 구현 상태

현재 코드는 **Places API (기존)**를 사용합니다:

```typescript
// Text Search
https://maps.googleapis.com/maps/api/place/textsearch/json

// Place Details
https://maps.googleapis.com/maps/api/place/details/json
```

## 📝 결론

**현재는 Places API (기존) 사용 유지 권장**

이유:
- ✅ 한국 약국은 100 million 데이터로 충분
- ✅ 안정적이고 검증된 API
- ✅ 현재 코드와 완벽 호환
- ✅ 문서와 커뮤니티 지원이 풍부

**Places API (New)는 나중에 필요할 때 전환 고려**

---

**참고**: 
- 두 API 모두 활성화 가능 (비용은 각각 발생)
- 필요시 나중에 New 버전으로 마이그레이션 가능

